# Database container configuration (PostgreSQL)
POSTGRES_DB=desk_management
POSTGRES_USER=desk_user
POSTGRES_PASSWORD=desk_password
POSTGRES_PORT=5432

# Backend SQLAlchemy database URL (used when running via Docker)
# To continue using SQLite even in Docker, leave this commented or set to the SQLite path.
# To use Postgres container, uncomment the following line:
# DATABASE_URL=postgresql+psycopg2://desk_user:desk_password@db:5432/desk_management

